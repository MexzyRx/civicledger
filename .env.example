# Server-side only. Create this in the Anthropic Console and configure it in
# Vercel Project Settings > Environment Variables. Never expose it with a
# NEXT_PUBLIC_ prefix or commit the real value.
ANTHROPIC_API_KEY=
