import Link from "next/link";
import { promises, projects } from "@/lib/data";
import { PromiseCard } from "@/components/ui";

export default function Home(){return <><section className="hero"><div className="hero-copy"><span className="kicker">Public promises, independently traceable</span><h1>What was promised?<br/><em>Show me the evidence.</em></h1><p>CivicLedger connects political commitments to real projects, documented progress and citizen observations—without partisan scores.</p><div className="location-card"><div><label>State</label><select defaultValue="rivers"><option value="rivers">Rivers State</option></select></div><div><label>Local government</label><select defaultValue="ph"><option value="ph">Port Harcourt City</option></select></div><Link href="/my-area" className="button">View my area →</Link></div><div className="trust-row"><span>✓ Evidence before judgement</span><span>✓ Sources on every assessment</span><span>✓ Uncertainty shown clearly</span></div></div><aside className="ledger-visual"><div className="seal">CL</div><p>THE PUBLIC RECORD</p><strong>{promises.length}</strong><span>promises tracked</span><strong>{projects.length}</strong><span>projects documented</span><strong>{projects.reduce((n,p)=>n+p.evidence.length,0)}</strong><span>sources reviewed</span></aside></section><section className="section"><div className="section-heading"><div><span className="kicker">Start with the record</span><h2>Recently reviewed promises</h2></div><Link href="/promises">View all promises →</Link></div><div className="card-grid">{promises.map(p=><PromiseCard key={p.slug} promise={p}/>)}</div></section><section className="ask-banner"><div><span className="kicker">Ask the public record</span><h2>Which education promises are still outstanding?</h2><p>Ask CivicLedger answers only from tracked records and always shows its sources.</p></div><Link href="/ask" className="button light">Ask CivicLedger →</Link></section></>}
