import { RepresentativeCard } from "@/components/ui";
import { representatives } from "@/lib/data";

export default function Representatives(){return <div className="page"><div className="page-intro"><span className="kicker">Your civic map</span><h1>Representatives</h1><p>These offices are resolved from the selected state, LGA, ward and corresponding electoral constituencies.</p></div><div className="representatives-directory">{(["Federal","State","Local"] as const).map(level=><section key={level}><div className="section-heading"><div><span className="kicker">{level} level</span><h2>{level==="Local"?"Port Harcourt City LGA":level==="State"?"Rivers State":"Federal Government"}</h2></div><span>{representatives.filter(item=>item.level===level).length} offices</span></div><div className="representative-grid directory-grid">{representatives.filter(item=>item.level===level).map(item=><RepresentativeCard key={item.slug} representative={item}/>)}</div></section>)}</div></div>}
